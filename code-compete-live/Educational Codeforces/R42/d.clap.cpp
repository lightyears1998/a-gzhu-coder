#include <bits/stdc++.h>
using namespace std;

// 对拍程序没有写完，写对拍程序的时候想到了一种解法 
// 写对拍程序的时候想到了一种解法 

const int maxN = 150150;

int num[maxN];

int main()
{
	int n; cin >> n;
	for (int i=0; i<n; ++i) {
		cin >> num[i];
	}	
	
	int cnt = n; bool change;
	do {
		
	} while (change);
	
	cout << cnt << endl;
	for (int i=0; i<n; ++i) {
		if (num[i]) cout << i << ' ';
	}
	cout << endl;
}
