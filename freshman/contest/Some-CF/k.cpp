#include <bits/stdc++.h>
using namespace std;

int find(string &s)
{
	string nxt;
	for (unsigned i=0; i<s.length(); ++i)
	{
		
	}
}

int main()
{
	string ans;
	while (cin >> ans)
	{
		printf("%d\n", find(ans));
	}
}
