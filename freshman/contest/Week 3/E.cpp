#include <bits/stdc++.h>
using namespace std;

const int maxN = 10050;

int n, m, r;
int d[maxN][maxN];

int main()
{
	int t; scanf("%d", &t);
	while (t--) 
	{
		memset(d, 0, sizeof(d));
		
		scanf("%d%d%d", &n, &m, &r);
		
	}
}
