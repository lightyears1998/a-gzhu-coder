#include <cstdio>
#include <algorithm>
using namespace std;

int main()
{
    int n;
    while (scanf("%d", &n) == 1)
    {
        long long mx = -1, mx2 = -1, tot = 0;

        for (int i = 1; i < n; i++)
        {

        }

        for ()

        if (mx + mx2 >= tot) printf("YES\n");
        else printf("NO\n");
    }
}
